<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_weight="10">

    <EditText
        android:hint="Enter Group Name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/group_name"
        android:layout_gravity="center_horizontal"
        android:inputType="text"
        android:imeOptions="actionDone"
        android:textColorHighlight="@color/tab_background"
        android:visibility="gone"
        />

    <com.getbase.floatingactionbutton.AddFloatingActionButton
        android:id="@+id/group_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="75dp"
        fab:fab_colorNormal="@color/tab_background"
        fab:fab_colorPressed="@color/tab_background_pressed"
        fab:fab_plusIconColor="@color/white" />

    <ListView
        android:id="@+id/lv_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/group_name"></ListView>
</RelativeLayout>